# Quality Standards

The Skill passes only if:

- all required files are present;
- execution logs reference actual output files;
- human review gates are represented;
- missing or conceptual-only components are labelled honestly.

Failure conditions:

- no execution log exists;
- Agent outputs are missing;
- Skill outputs cannot be traced;
- human review is claimed but no record exists.

