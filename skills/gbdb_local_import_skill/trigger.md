# Trigger Conditions

Call this Skill when:

- GBDB data are needed alongside PBDB;
- local curated data must be added;
- fields differ across sources and require mapping.

Do not call this Skill when:

- only PBDB data are used;
- the task is taxonomic interpretation;
- local data cannot be disclosed even in derived form.

