# Quality Standards

The Skill passes only if:

- source provenance columns are present;
- no source table is overwritten;
- all mapped fields are documented;
- missing critical fields are reported;
- combined data can be traced back to the original source file.

Failure conditions:

- source IDs are lost;
- local and public records are merged without source labels;
- schema mapping is implicit rather than documented.

