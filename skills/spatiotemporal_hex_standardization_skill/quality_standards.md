# Quality Standards

The Skill passes only if:

- grid parameters are recorded;
- failed assignments are exported;
- sampling intensity is summarized;
- time-bin and grid-cell fields are both present;
- standardization parameters are reproducible.

Failure conditions:

- coordinate filtering is undocumented;
- sparse cells are interpreted as true absences;
- grid resolution changes main results without sensitivity report.

