# Trigger Conditions

Call this Skill when:

- origination/extinction rate estimates are needed;
- PyRate input tables have already been prepared;
- the workflow has enough temporal resolution for rate modelling.

Do not call this Skill when:

- occurrence ages are too broad;
- taxonomic cleaning is unresolved;
- PyRate runtime dependencies are missing;
- only descriptive diversity is required.

