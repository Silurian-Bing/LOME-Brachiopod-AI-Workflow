# Quality Standards

The Skill passes only if:

- figure inputs are listed;
- figure script path is recorded;
- vector and raster outputs are produced;
- caption does not overstate results;
- visual transformations are documented.

Failure conditions:

- figure cannot be regenerated;
- data are manually altered;
- uncertainty is hidden;
- plotting choices create a misleading spatial or temporal pattern.

