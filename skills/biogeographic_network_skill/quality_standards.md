# Quality Standards

The Skill passes only if:

- assemblage matrix construction is documented;
- similarity metric is recorded;
- sampling thresholds are explicit;
- network edges can be traced to shared taxa;
- sensitivity to widespread taxa is considered.

Failure conditions:

- singleton regions dominate the network;
- broad-ranging taxa drive all similarity without warning;
- clusters are named as provinces without expert review.

